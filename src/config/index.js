const config = {}

// Mongo DB connection config
config.dbUser = 'tevor'
config.dbPassword = 'secret'
config.dbHost = 'localhost'
config.dbPort = '27017'
config.dbName = 'tevorAva'

// Logger config
config.logDir = './logs'
config.logFile = 'app.log'

export default config
